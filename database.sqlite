-- DDL
create table aluno (
aluno_id integer primary key autoincrement,
nome varchar(255),
email varchar(255)
)

-- DDL
alter table aluno
add matricula varchar(255)

-- DDL drop table aluno

-- DML
insert into aluno(nome, email, matricula)
values('fulano', 'fulano@gmail.com', '001245'),
       ('siclano', 'siclano@gmail.com', '003451'),
       ('beltrano', 'beltrano@gmail.com', '001463');

select nome,matricula from aluno
where aluno_id in (2,3)
--where nome = 'beltrano'

update aluno
set nome = 'carlinhos'
where aluno_id = 3

delete from aluno
where nome = 'fulano'

alter table aluno
add genero varchar(10)

alter table aluno
add idade varchar(10)

update aluno
set genero = 'masculino', idade = 20
where aluno_id in (2,3)

insert into aluno(nome, email, matricula, genero, idade)
values('stefany', 'stefany@gmail.com', '001472', 'feminino', 23)

select * from aluno

select count(*) from aluno

select sum(idade) from aluno

select avg(idade) from aluno